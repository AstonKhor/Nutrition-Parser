import React from 'react';

//add option for more complex queries such as a range
//clean up UI to make it more pleasant

let SearchBox = ({ queries, clearQueries }) => {
  return (
      <div>
      </div>
  )
}

export default SearchBox;