import React from 'react';
import Style from '../../styled-components/Wrapper';

let Header = ({ nutrients, queries, addQuery, clearQueries, handleSearch }) => {
  return (
      <Style.Header>
        Header stuff
      </Style.Header>
  )
}

export default Header;